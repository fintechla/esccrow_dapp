import styled from "styled-components";

export const Main = styled.main`
  padding: 32px 0;
  min-height: calc(100vh - 220px);
`;

export const BetaBlock = styled.div`
  text-align: center;
  color: var(--dark);
  background-color: var(--sky-blue);
  padding: 5px;
  box-shadow: rgba(0, 0, 0, 0.1) 5px 5px 5px;
  font-size: 0.9rem;
`;
